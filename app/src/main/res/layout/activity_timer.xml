<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:context="me.kevinxchan.kevinxchan.stretchit.TimerActivity">

    <android.support.design.widget.AppBarLayout
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
                android:id="@+id/activity_timer_toolbar"
                app:titleTextColor="@color/white"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:minHeight="?attr/actionBarSize"
                android:background="?attr/colorPrimary"/>
    </android.support.design.widget.AppBarLayout>

    <include layout="@layout/content_timer"/>

    <android.support.design.widget.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/fab_start"
            app:srcCompat="@drawable/ic_play_arrow_white_24dp"
            app:backgroundTint="@color/colorPrimaryDark"
            app:fabSize="mini"
            android:elevation="0dp"
            android:layout_gravity="bottom|start"
            android:layout_margin="@dimen/fab_timer_margin"/>

    <android.support.design.widget.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/fab_pause"
            app:srcCompat="@drawable/ic_pause_white_24dp"
            app:backgroundTint="@color/colorPrimaryDark"
            app:fabSize="mini"
            android:elevation="0dp"
            android:layout_gravity="bottom|center"
            android:layout_margin="@dimen/fab_timer_margin"/>

    <android.support.design.widget.FloatingActionButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/fab_stop"
            app:srcCompat="@drawable/ic_stop_white_24dp"
            app:backgroundTint="@color/colorPrimaryDark"
            app:fabSize="mini"
            android:elevation="0dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/fab_timer_margin"/>

</android.support.design.widget.CoordinatorLayout>